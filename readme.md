encode your email and password it first by running:
```
python3 encode.py
```
then when you will get ciphered bytes, create secret.py with content, and fill:
```
sender_mail = b'xddd'
sender_pass = b'xd'
```